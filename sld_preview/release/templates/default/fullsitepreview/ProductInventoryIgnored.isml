<iscontent type="text/html" charset="UTF-8" compact="true">

<isif condition="#(isDefined(previewAttribute)) AND (previewAttribute:ID EQ "ProductInventoryIgnored") AND NOT(previewAttribute:Default)#">
	<div class="sfe-preview-summary-parameter-group-nondefault">
	    <isif condition="#previewAttribute:SelectedValue:ID EQ 'true'#">
		     <istext key="ProductInventoryIgnored.AllProductsAreBuyable"/>	     
		<iselse>
			 <istext key="ProductInventoryIgnored.ProductBuyableStatusIsNotOverridden."/>
	  	</isif>
  	</div>
</isif>