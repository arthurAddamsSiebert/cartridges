import "enfinity:/core/edl/com/intershop/beehive/core/types.edl";

namespace com.intershop.beehive.core.capi.common
{
    /*
     * This is the base interface for all Providers.
     * 
     * <p>Clients may create an implementation of this interface. To ensure compatibility in future releases the class implementing this interface should be a subclass of <code>ProviderImpl</code>.
     * 
     * @deprecated Since 7.3 the legacy NamingMgr-based handling has been superseded by dependency-injection. This interface causes a (now unnecessary) dependency on cartridge core.
     */
    cartridge interface Provider
    {
    }
}
